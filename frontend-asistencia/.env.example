# Configuración de entorno para el frontend

# URL base del backend.
# - En desarrollo local: dejar vacío para usar el proxy de Vite (vite.config.js)
#   que redirige /api y /qr hacia http://localhost:8080
# - En producción: usar la URL pública del backend desplegado en Render.
#   Ejemplo: https://control-asistencia-backend-s1u5.onrender.com
VITE_API_URL=