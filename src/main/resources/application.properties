spring.application.name=control-asistencia-backend

# Configuración de la Conexión a MySQL
# Las credenciales se cargan desde variables de entorno (obligatorio)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/control_asistencia?serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# URL Base para la generacion de codigos QR
app.qr.base-url=${APP_QR_BASE_URL:http://localhost:8080}